<shape xmlns:android="http://schemas.android.com/apk/res/android" >

  	<solid android:color="@color/bg_white"/>
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/line_fl_text" />
    <!-- 圆角 -->
    <corners
       android:radius="@dimen/x10"/>

</shape>